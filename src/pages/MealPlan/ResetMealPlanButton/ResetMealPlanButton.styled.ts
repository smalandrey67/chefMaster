import styled from 'styled-components'

export const ResetMealPlanWrapper = styled.div`
  display: flex;
  justify-content: flex-end;
  margin-bottom: 10px;
`
export const ResetMealPlanButtonEl = styled.button`
  background-color: transparent;
  margin: 0;
  padding: 0;
  color: var(--color-error);
`
