import { MotionType } from '../types/Motion'

export const motionSettings: MotionType = {
   animate: { opacity: 1 },
   initial: { opacity: 0 },
   exit: { opacity: 0 },
   transition: { duration: 0.5 }
}
